# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only secret (never commit)
SUPABASE_SECRET_KEY=

# Public site URL (used for sitemap, robots.txt, OG metadata).
# Defaults to the Vercel domain if unset.
NEXT_PUBLIC_SITE_URL=https://mietcheck-map.vercel.app
